# Telegram Bot Module

Telegram бот для системы тестирования, написанный на C++.

## Функциональность

- ✅ Авторизация пользователей (логин/пароль и код по email)
- ✅ Просмотр списка доступных тестов
- ✅ Прохождение тестов
- ✅ Сохранение результатов
- ✅ Использование Redis для хранения сессий
- ✅ Интеграция с auth_module и central_module

## Технологии

- **C++17** - основной язык
- **libcurl** - HTTP запросы к API
- **jsoncpp** - работа с JSON
- **hiredis** - работа с Redis
- **Docker** - контейнеризация
- **NGINX** - проксирование (опционально)

## Сборка и запуск

Бот автоматически собирается и запускается через Docker Compose:

```bash
docker-compose up -d telegram_bot
```

## Конфигурация

Токен бота задан в коде: `8097020213:AAFTYeWmWules9nMTa2eazDc-r3Q5ZzboMs`

Для изменения конфигурации отредактируйте константы в `main.cpp`:
- `AUTH_API` - URL модуля авторизации
- `CENTRAL_API` - URL центрального модуля
- `REDIS_HOST` и `REDIS_PORT` - параметры Redis

## Использование

1. Найдите бота в Telegram по токену или username
2. Отправьте `/start` для начала работы
3. Выберите способ авторизации:
   - 🔐 Войти по логину и паролю
   - 📧 Войти по коду
4. После авторизации доступны:
   - 📋 Список тестов
   - 📊 Мои результаты (в разработке)
   - 🚪 Выйти

## Redis

Бот использует Redis для:
- Хранения сессий пользователей (TTL 1 час)
- Кэширования данных
- Синхронизации состояния между перезапусками

Ключи в Redis:
- `tg_session:{chat_id}` - сессия пользователя

## API интеграция

Бот взаимодействует с:
- `auth_module` - для авторизации пользователей
- `central_module` - для получения тестов и отправки результатов

## Примечания

- Бот работает в режиме long polling (получение обновлений)
- Сессии хранятся в Redis с TTL 3600 секунд
- Административные функции недоступны в боте (только пользовательские)
